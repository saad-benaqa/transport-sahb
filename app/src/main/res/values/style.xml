<?xml version="1.0" encoding="utf-8"?>
<!-- res/values/styles.xml -->
<!-- res/values/styles.xml -->
<!-- res/values/styles.xml -->
<resources>

    <!-- Style pour le TextView du nom de la ville -->
    <style name="VilleTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:paddingBottom">4dp</item>
    </style>

    <!-- Style pour le TextView de la date -->
    <style name="DateTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@android:color/white</item>



    </style>

    <style name="BottomNavigationIconSize">
        <item name="android:layout_width">320dp</item> <!-- Largeur de l'icône -->
        <item name="android:layout_height">320dp</item> <!-- Hauteur de l'icône -->
    </style>

    <style name="NavigationIconStyle">
        <item name="android:tint">@color/menu_icon_color</item>
    </style>





    <style name="BottomNavigationStyle" parent="Widget.Design.BottomNavigationView">
        <item name="android:textSize">120sp</item> <!-- Taille du texte pour les labels -->
        <item name="android:maxLines">1</item> <!-- Nombre maximum de lignes pour les labels -->
    </style>


    <!-- Style pour le TextView de la description -->
    <style name="DescriptionTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Style pour le conteneur de l'élément -->
    <style name="ItemContainer">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">16dp</item>
        <item name="android:background">@drawable/bg1</item>
    </style>

</resources>


